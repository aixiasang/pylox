// 解析器错误检测测试
// 这个文件包含多个应该被解析器检测到的错误

// 1. 测试变量自引用
print "测试1：变量自引用 - 应该报错";
var a = a; // 错误：不能在变量自己的初始化器中引用自己

// 2. 测试重复变量声明
print "测试2：同一作用域内重复声明变量 - 应该报错";
{
  var b = 1;
  var b = 2; // 错误：已经在此作用域声明了变量"b"
}

// 3. 测试在函数外使用return
print "测试3：在函数外使用return - 应该报错";
return; // 错误：不能在函数外使用return

// 4. 测试未声明变量
print "测试4：使用未声明的变量 - 应该报错";
print undeclared_var; // 错误：未定义的变量

// 如果你运行这个文件，应该会看到解析器检测到多个错误
// 这说明我们的静态分析阶段工作正常