// 测试挑战1：不同类型的比较
print "---- 挑战1：不同类型的比较 ----";
print 3 < "pancake";  // 数字 < 字符串，应该返回true
print "apple" > 5;    // 字符串 > 数字，应该返回true
print "zebra" < "apple";  // 字符串比较，应该返回false
print 10 > 5;         // 数字比较，应该返回true

// 测试挑战2：字符串与数字的连接
print "---- 挑战2：字符串与数字的连接 ----";
print "scone" + 4;    // 应该返回 "scone4"
print 4 + "scone";    // 应该返回 "4scone"
print "年龄: " + 25 + "岁";  // 应该返回 "年龄: 25岁"
print 2 + 3 + " 是 " + 2 * 3 + " 与 " + 5 + 1;  // 应该返回 "5 是 6 与 51"

// 测试挑战3：除以零
print "---- 挑战3：除以零 ----";
// 下面的代码应该产生运行时错误，但不会导致程序崩溃
// print 10 / 0;  // 注释掉以便看到其他输出

// 附加测试：不同类型的嵌套比较
print "---- 附加测试：复杂表达式 ----";
print (3 < "pancake") and (10 > 5);  // 应该返回true
print (3 < "pancake") or (10 < 5);   // 应该返回true
print ("apple" > 5) and ("zebra" < "apple");  // 应该返回false 